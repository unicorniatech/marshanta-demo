# Environment template for Marshanta
# Copy to .env and fill values as needed

# App
NODE_ENV=development
PORT=3000

# Auth (do not commit real secrets)
AUTH_SECRET=change_me
MOCK_WEBHOOK_SECRET=dev_secret # used by mock payments webhook signature

# Database
DB_DRIVER=memory # set to "pg" to use Postgres adapter
# Pooled URL used by the API at runtime (Supabase pooled connection recommended)
DATABASE_URL=postgresql://user:password@localhost:5432/marshanta?sslmode=require
# Direct URL used for migrations (use primary connection string)
DIRECT_URL=postgresql://user:password@localhost:5432/marshanta?sslmode=require
# Notes:
# - If your password contains special characters (e.g., @), URL-encode it.
# - For Supabase, use the pooled URL for DATABASE_URL and the direct URL for DIRECT_URL.

# Web
PUBLIC_APP_NAME=Marshanta
